# Synthetic Spotify data

Generated by `src/synthetic_spotify.py` to develop the pipeline before the real
Extended Streaming History arrives from Spotify (requested 18 Jan, expected
within 30 days).

The schema mirrors what the real export looks like according to Spotify's
documentation. Field names and types should match so that the parser written
against this data also works on the real export.
